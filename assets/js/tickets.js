require("bootstrap");
const createEl = require("./domMethods");

if (window.locations.href.indexOf("tickets") > -1) {
    $(document).ready( function() {
        // DOM manipulation code specific to each page.
        })
}